<UserControl x:Class="SalaryManager.Prism.Views.Option"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:UserControls="clr-namespace:SalaryManager.Prism.Views.Child"
             xmlns:ViewModels="clr-namespace:SalaryManager.Prism.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <TabControl>
            <TabItem Header="全般">
                <UserControls:GeneralOption />
            </TabItem>

            
            <TabItem Header="スプレッドシート">
                <UserControls:SpreadSheetOption />
            </TabItem>

            <TabItem Header="PDF">
                <UserControls:PDFOption />
            </TabItem>

            <TabItem Header="カレンダー">
                <UserControls:CalendarOption />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
