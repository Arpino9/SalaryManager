<UserControl x:Class="SalaryManager.Prism.Views.Child.CalendarOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             xmlns:ViewModels="clr-namespace:SalaryManager.Prism.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.DataContext>
        <ViewModels:CalendarOptionViewModel/>
    </UserControl.DataContext>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Content="JSONのパス"/>

        <TextBox Grid.Column="1" 
             Text="{Binding SelectPrivateKey_Text}" 
             IsEnabled="False" >
        </TextBox>

        <Button Content="開く" 
            Grid.Column="2"                 
            Command="{Binding SelectPrivateKey_Command}">
        </Button>

        <Label Content="カレンダーID"
           Grid.Row="1"/>

        <TextBox Grid.Column="1" 
             Grid.Row="1"
             Text="{Binding SelectCalendarID_Text}" >
        </TextBox>
    </Grid>
</UserControl>
